# Disabled Vercel Python entry

This folder used to live at `/api`, which made Vercel deploy FastAPI and return
`{"detail":"Not Found"}` for the site homepage.

- Frontend: Vercel (`frontend/`, see root `vercel.json`)
- Backend: Railway (`backend/`)
