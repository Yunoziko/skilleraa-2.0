# Skilleraa frontend

React (CRA + CRACO) UI for Skilleraa.

See the root [README.md](../README.md) for setup, env vars, and [DEPLOYMENT.md](../DEPLOYMENT.md) for Vercel.

```bash
cp .env.example .env   # fill REACT_APP_* values
npm install
npm start
npm run lint
CI=true npm run build
```
